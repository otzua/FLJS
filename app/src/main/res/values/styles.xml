<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Main app theme — DayNight automatically handles light/dark -->
    <style name="AppTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowBackground">@color/colorBackground</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
        <item name="android:fontFamily">sans-serif</item>
        <!-- No global WindowAnimStyle: each activity calls overridePendingTransition
             for the correct directional slide. A global fade here fights those calls
             and makes every back/forward look wrong. -->
    </style>

    <!-- Preference theme -->
    <style name="PreferenceTheme" parent="PreferenceThemeOverlay">
        <item name="android:layout">@layout/activity_settings</item>
    </style>

</resources>
